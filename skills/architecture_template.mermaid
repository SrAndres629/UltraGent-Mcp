graph TD
    %% ULTRAGENT ARCHITECTURE MAP (Spatial Memory)
    %% Usage: Copy this structure when analyzing new systems.
    
    %% Style Definitions (Visual Hierarchy)
    classDef core fill:#ff9900,stroke:#333,stroke-width:2px,color:white;
    classDef agent fill:#2196f3,stroke:#333,stroke-width:2px,color:white;
    classDef memory fill:#9c27b0,stroke:#333,stroke-width:2px,color:white;
    classDef tool fill:#4caf50,stroke:#333,stroke-width:1px,color:white;

    %% CORE SYSTEM (The Brain)
    subgraph Brain [Wait.. Thinking System]
        Router[Omni-Router]:::core
        Cortex[Cortex Memory]:::memory
        Vision[Vision System]:::core
    end

    %% AGENTS ( The Hands)
    subgraph Agents [Execution Layer]
        Mechanic[Mechanic (Orchestrator)]:::agent
        Evolution[Evolution (Auditor)]:::agent
        Scout[Scout (Researcher)]:::agent
    end

    %% DATA FLOW
    Mechanic -->|Consults| Router
    Mechanic -->|Reads/Writes| Cortex
    Evolution -->|Audits| Mechanic
    Scout -->|Feeds| Cortex

    %% CRITICAL PATHS
    Router -->|Inference| External_LLMs[Cloud AI]
    Vision -->|Maps| Project_Files[Codebase]
